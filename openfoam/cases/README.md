# Optional Case Directory

Generated OpenFOAM teaching cases can be placed here. These cases are optional and are not used by the Python-only classroom workflow.

Keep case outputs out of version control unless an instructor intentionally adds a small, reviewed template.
